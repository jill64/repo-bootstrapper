{
  "type": "module",
  "scripts": {
    "build": "bunx wrangler deploy --dry-run",
    "deploy": "bunx wrangler deploy",
    "lint": "eslint . && tsc",
    "format": "bunx prettier -w ."
  },
  "prettier": "@jill64/prettier-config",
  "devDependencies": {
    "@jill64/eslint-config-ts": "2.0.18",
    "@jill64/prettier-config": "1.0.0",
    "octoflare": "2.1.6",
    "typescript": "5.8.2",
    "eslint": "9.22.0"
  }
}
